import React from 'react';
import styles from './Documentation.module.css';

const Documentation: React.FC = () => {
  return (
    <div className={styles.container}>
      <h1 className={styles.title}>UI Toolkit Documentation</h1>
      
      <section className={styles.section}>
        <h2 className={styles.sectionTitle}>Getting Started</h2>
        <p>To start using the UI Toolkit, first install the required dependencies:</p>
        <pre className={styles.code}>
          {`npm install react-router-dom`}
        </pre>
      </section>

      <section className={styles.section}>
        <h2 className={styles.sectionTitle}>Usage Guidelines</h2>
        <p>To use a component, import it into your project:</p>
        <pre className={styles.code}>
          {`import Button from './components/Button/Button';`}
        </pre>
        <p>Then include it in your JSX:</p>
        <pre className={styles.code}>
          {`<Button>Click Me</Button>`}
        </pre>
      </section>

      <section className={styles.section}>
        <h2 className={styles.sectionTitle}>Components</h2>
        
        <h3 className={styles.subsectionTitle}>Button</h3>
        <p>Buttons are used to trigger actions:</p>
        <pre className={styles.code}>
          {`import Button from './components/Button/Button';

<Button>Click Me</Button>`}
        </pre>

        <h3 className={styles.subsectionTitle}>Navbar</h3>
        <p>The Navbar is used for navigation:</p>
        <pre className={styles.code}>
          {`import Navbar from './components/Navbar/Navbar';

<Navbar />`}
        </pre>

        <h3 className={styles.subsectionTitle}>Hero Section</h3>
        <p>The Hero Section is used for highlighting key content:</p>
        <pre className={styles.code}>
          {`import HeroSection from './components/HeroSection/HeroSection';

<HeroSection />`}
        </pre>

        <h3 className={styles.subsectionTitle}>Card Section</h3>
        <p>The Card Section is used for displaying information in card format:</p>
        <pre className={styles.code}>
          {`import CardSection from './components/CardSection/CardSection';

<CardSection />`}
        </pre>

        <h3 className={styles.subsectionTitle}>Dropdown</h3>
        <p>Dropdowns are used to display a list of options:</p>
        <pre className={styles.code}>
          {`import Dropdown from './components/Dropdown/Dropdown';

<Dropdown />`}
        </pre>

        <h3 className={styles.subsectionTitle}>Footer</h3>
        <p>The Footer is used for site-wide information:</p>
        <pre className={styles.code}>
          {`import Footer from './components/Footer/Footer';

<Footer />`}
        </pre>
      </section>

      <section className={styles.section}>
        <h2 className={styles.sectionTitle}>Responsive Design</h2>
        <p>All components are designed to be responsive and work across various devices.</p>
        <p>To ensure responsiveness, use the provided CSS classes and media queries.</p>
      </section>

      <section className={styles.section}>
        <h2 className={styles.sectionTitle}>Code Copy Functionality</h2>
        <p>Each component comes with a code copy functionality. Click the copy button to copy the code snippet to your clipboard.</p>
      </section>
    </div>
  );
};

export default Documentation;
